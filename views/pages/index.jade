extends ../layout

block content
	.container
		.row
			each item in movie
				.col-md-2
					.thumbnail
						a(herf="/detail/#{item._id}")
							img(src="", alt="#{item.title}")
						.coaption
							h3 #{item.title}
							p: a.btn.btn-primary(href="detail/#{item._id}", role="button") 观看预告片